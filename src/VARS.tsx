export const API_HOST: string = window.location.hostname;
export const API_PROTOCOL: string = window.location.protocol;
export const API_PORT: string = (window.location.port != "" ? ":"+window.location.port : "")

export const GMAPS_API_KEY: string = ""